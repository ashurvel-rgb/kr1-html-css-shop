# Учебный frontend-проект

## Описание

Проект выполняется в рамках дисциплины «Фронтенд и бэкенд разработка».

Тема проекта: многостраничный сайт интернет-магазина или каталога услуг.

## Структура проекта

- `index.html` — главная страница;
- `css/style.css` — файл стилей;
- `js/main.js` — логика модального окна и валидации формы;
- `images/` — папка для изображений;
- `.gitignore` — список файлов, исключённых из Git;
- `README.md` — описание проекта.

## Используемые технологии

- HTML;
- CSS;
- Git;
- GitHub.

## Автор

- ФИО: Велиханов Ашурбег Мадрудинович
- Группа: ЭФБО 17-25

## Текущий статус

Практическая работа №4: CSS-файл упорядочен, добавлены переменные и состояния интерфейса.

## Что сделано

- Добавлены шапка, навигация, hero-блок, преимущества, три карточки товаров, aside и подвал.
- Добавлены базовые стили, минимальный сброс и `box-sizing: border-box`.
- Добавлены форма заявки, модальное окно и JavaScript-валидация.
- Цвета, отступы и скругления вынесены в CSS-переменные.
- Добавлены состояния `:hover`, `:focus-visible`, `:disabled` и подсветка ошибок.

## Как проверить

- Открыть `index.html` в браузере.
- Проверить, что все разделы отображаются, а ссылки меню ведут к нужным блокам.
- Проверить, что CSS-файл подключён корректно.

## Ссылка на опубликованный проект

GitHub Pages: https://ashurvel-rgb.github.io/kr1-html-css-shop/

## Репозиторий и Pull Request

- [Репозиторий GitHub](https://github.com/ashurvel-rgb/kr1-html-css-shop).
- [Объединённый Pull Request №1](https://github.com/ashurvel-rgb/kr1-html-css-shop/pull/1).
- [Изменения в Pull Request](https://github.com/ashurvel-rgb/kr1-html-css-shop/pull/1/files).

## Проверка в Visual Studio Code

1. Открыть папку проекта `kr1-html-css-shop`.
2. Показать `index.html`: семантическая структура, навигация и карточки товаров.
3. Показать `css/style.css`: базовое оформление и стили секций.
4. Показать `.gitignore` и описание проекта в `README.md`.
5. В терминале VS Code выполнить команды:

```sh
git status
git log --oneline --graph --decorate
git remote -v
```

6. Открыть ссылки на Pull Request и опубликованный сайт из этого README.

GitHub Pages настроен на публикацию из ветки `main`, папка `/ (root)`.
Папка `images/` подготовлена для следующих занятий; файл `.gitkeep` сохраняет её в Git.

## Постановка контрольной работы №1

Тема контрольной работы №1: «HTML и CSS: разработка многостраничного сайта».

В рамках контрольной работы необходимо разработать многостраничный HTML/CSS-сайт интернет-магазина или каталога услуг.

Проект должен включать:

- главную страницу;
- страницу каталога;
- страницу карточки товара или услуги;
- страницу оформления заявки;
- страницу контактов;
- семантическую HTML-разметку;
- внешний CSS-файл;
- базовую навигацию;
- оформление основных блоков страницы.

## История выполнения

- Практическая работа 1: создан Git-репозиторий, опубликован проект на GitHub Pages.
- Практическая работа 2: создан HTML-каркас стартовой страницы и выполнено базовое CSS-оформление.
- Практическая работа 3: добавлена форма заявки, модальное окно и базовая валидация.
- Практическая работа 4: добавлены CSS-переменные, состояния интерфейса и упорядочена структура стилей.

## CSS-архитектура

В проекте используются:

- CSS-переменные в `:root`;
- единая шкала отступов;
- переменные для цветов и скруглений;
- состояния `:hover`, `:focus-visible`, `:disabled`;
- визуальная подсветка ошибочных полей через `aria-invalid`;
- структурированный файл `css/style.css`.

## Реализованные элементы интерфейса

- кнопки заказа в карточках товаров;
- модальное окно с формой заявки;
- поля формы: имя, e-mail, телефон, дата, тема, комментарий и согласие;
- базовая HTML-валидация;
- базовая JavaScript-обработка формы;
- сообщение об успешной отправке.

## Pull Request практической работы №2

[Add semantic HTML structure and base CSS styles — PR №2](https://github.com/ashurvel-rgb/kr1-html-css-shop/pull/2).

## Что показать по практической работе №2

- `index.html`: один `main`, элементы `header`, `nav`, `section`, `article`, `aside`, `footer`.
- `css/style.css`: внешний файл, сброс `margin`, модель `border-box`, оформление основных блоков.
- В браузере: меню, переход к товарам, три карточки, преимущества и подвал.
- В README: постановку контрольной работы №1 и историю выполнения.
- В Git: ветку `feature/html-css-start-page`, историю коммитов и Pull Request.

В модели `content-box` отступы и рамки добавляются к ширине; в `border-box` они уже входят в неё. `padding` — внутренний отступ, `margin` — внешний.
